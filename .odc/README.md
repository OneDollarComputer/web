# `.odc/` — local agent bridge (One Dollar Computer)

Committed: this README. **Not committed:** `.odc/local/` (gitignored).

Hub orchestrates the system map (`~/github/hub`). This repo owns the **site / editor / curriculum**.

Agents: read `.odc/local/` first; when contracts or flows change, update hub or write `.odc/local/HUB.md`.

Full convention: `hub/.odc/README.md`.
