# Legacy C editor snapshot

Snapshot of the browser editor before migration to the **bootloader_odb** Rust stack (`use odc::*;`).

Kept for reference only. Live app is `editor/index.html` one level up.
